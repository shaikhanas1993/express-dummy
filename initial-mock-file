import { initialUserState } from 'src/core/context/user-store/user-reducer';
import { UserContext } from 'src/core/context/user-store/user-store';

export const initialUserStoreContext: UserContext = {
  state: initialUserState,
  setUserId: jest.fn(),
  setJwtData: jest.fn(),
  refreshJwt: jest.fn(),
  signOut: jest.fn(),
  setUserDetails: jest.fn(),
};
